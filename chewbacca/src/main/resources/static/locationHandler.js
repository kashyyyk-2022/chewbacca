/* may be redundant

function getLocation(longOrLat) {
    var cookie = getCookie('locationPermission',false);
    if (cookie == false) {

    }
    <!--
            if(permission == false) Ask permission to access user location
                   deny: reload index
                   allow: check if gps is turned on.
                        if not turned on: ask to turn on.
                            else get location: save longitude and latitude in-->
}

*/
